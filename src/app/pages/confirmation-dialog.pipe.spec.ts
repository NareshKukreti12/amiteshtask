import { ConfirmationDialogPipe } from './confirmation-dialog.pipe';

describe('ConfirmationDialogPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmationDialogPipe();
    expect(pipe).toBeTruthy();
  });
});
